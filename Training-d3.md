Here is the text converted into a clean, organized Markdown format.

# Home Work - 3rd Feb 2026

### Functional Programming & Closures

* Write a function that filters even numbers from an array using a closure.
* Create a custom reduce function that combines elements of an array.
* Chain `filter`, `map`, and `reduce` to process an array of integers.
* Convert a string of digits into an array of integers using higher-order functions.
* Check if all characters in a string are numeric using functional programming.

---

### Strings & Characters (Coding-Focused)

* Write a function that counts the number of characters in a given string.
* Convert a string into an array of characters.
* Convert a string into an array of strings where each element represents one character.
* Given a sentence, count the number of words in it.
* Write code to reverse a string without using built-in `reversed()` directly on String.
* Check whether a given string is a palindrome.
* Given a string, count the number of vowels and consonants.
* Write code to remove all whitespaces from a string.
* Given a string, return a dictionary with the frequency of each character.
* Compare two strings and check if they contain the same characters (order does not matter).
* Given a string containing mixed letters and digits, separate letters and digits into two arrays.
* Extract the first and last character of a string safely.
* Given a sentence, capitalize the first letter of every word.
* Write code to check whether a string contains only numeric characters.
* Convert a string of comma-separated values into an array of strings.

### Collection Types – Array (Coding-Focused)

* Create an array of integers and print all elements using a loop.
* Write a function to find the maximum and minimum value in an integer array.
* Remove duplicate values from an array while preserving order.
* Given an array of integers, separate even and odd numbers into two arrays.
* Reverse an array without using `reversed()`.
* Find the sum of all elements in an array.
* Sort an array of strings alphabetically.
* Given an array of optional strings, return an array with non-nil values only.
* Merge two arrays and remove duplicates.
* Find the second largest number in an array.

### Collection Types – Set (Coding-Focused)

* Create a set of strings and insert new values into it.
* Demonstrate how duplicate values behave in a set.
* Find common elements between two sets.
* Find elements that exist in one set but not in the other.
* Find elements that are not common between two sets.
* Convert an array into a set and back into an array.
* Use a set to remove duplicate values from a list of strings.
* Check whether a set contains a specific value.
* Given two sets, check if one is a subset of the other.
* Use a set to track unique user selections.

### Collection Types – Dictionary (Coding-Focused)

* Create a dictionary with string keys and integer values.
* Access a value from a dictionary safely.
* Add a new key-value pair to a dictionary.
* Update an existing value in a dictionary.
* Remove a key-value pair from a dictionary.
* Iterate over all keys and values in a dictionary.
* Count the frequency of elements in an array using a dictionary.
* Convert two arrays (keys and values) into a dictionary.
* Given a dictionary, find the key with the highest value.
* Sort a dictionary by its values.

### Mixed Practice (Real iOS Scenarios)

* From a list of user names, remove duplicates and display them in sorted order.
* Given a list of product prices as strings, convert them into numbers and calculate the total.
* From a sentence, find the most frequently occurring word.

---
